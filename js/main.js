// Company card click (placeholder)
document.querySelectorAll('.portfolio-card').forEach(card => {
  card.addEventListener('click', () => {
    alert('This will open your work for this company.');
  });
});
